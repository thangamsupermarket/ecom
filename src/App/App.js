import * as React from 'react';
import Routing from '../Routes/MainRouter';

const RootComponent = (props) => {
    return ( 
        <React.Fragment>
            <Routing />
        </React.Fragment>
     );
}
 
export default RootComponent;